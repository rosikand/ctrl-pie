# ruckig 0.15.3 still declares deprecated cmake.targets. i2rt's own uv build
# constraint requires the older backend; PIP_CONSTRAINT carries this into
# pip's isolated PEP 517 build process.
scikit-build-core<0.10
